version: '3.8'
services:
  web:
    build:
      context: ./
      target: production
    volumes:
      - .:/src
    command: npm run start
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      DEBUG: nodejs-docker-express:*